{
  "name": "arab-automators-telegram-foundation",
  "version": "1.0.0",
  "description": "n8n Telegram workflow foundation: config from Postgres, update normalizer, staff roles",
  "private": true,
  "scripts": {
    "test": "node tests/run-tests.js"
  },
  "engines": { "node": ">=18" }
}
